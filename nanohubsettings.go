// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/open-uem/ent/nanohubsettings"
)

// NanoHubSettings is the model entity for the NanoHubSettings schema.
type NanoHubSettings struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// NanoHub API base URL (e.g. https://nanohub.example.com:10000/api/v1)
	ServerURL string `json:"server_url,omitempty"`
	// NanoHub API basic auth username
	Username string `json:"username,omitempty"`
	// NanoHub API basic auth password
	Password string `json:"password,omitempty"`
	// CA certificate PEM content used by NanoHub for webhook TLS verification
	CaCerFile string `json:"ca_cer_file,omitempty"`
	// SCEP server URL for Apple device certificate enrollment
	ScepURL string `json:"scep_url,omitempty"`
	// SCEP challenge password
	ScepChallenge string `json:"scep_challenge,omitempty"`
	// NanoHub MDM endpoint URL that Apple devices connect to (e.g. https://mdm.example.com:9004/mdm)
	MdmURL string `json:"mdm_url,omitempty"`
	// MDM Vendor RSA private key in PEM format (provided by OpenUEM)
	VendorPrivateKeyPem string `json:"-"`
	// MDM Vendor certificate in PEM format (provided by OpenUEM)
	VendorCertPem string `json:"vendor_cert_pem,omitempty"`
	// MDM enrollment profile PayloadIdentifier used in .mobileconfig and RemoveProfile command
	EnrollmentProfileID string `json:"enrollment_profile_id,omitempty"`
	selectValues        sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*NanoHubSettings) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case nanohubsettings.FieldID:
			values[i] = new(sql.NullInt64)
		case nanohubsettings.FieldServerURL, nanohubsettings.FieldUsername, nanohubsettings.FieldPassword, nanohubsettings.FieldCaCerFile, nanohubsettings.FieldScepURL, nanohubsettings.FieldScepChallenge, nanohubsettings.FieldMdmURL, nanohubsettings.FieldVendorPrivateKeyPem, nanohubsettings.FieldVendorCertPem, nanohubsettings.FieldEnrollmentProfileID:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the NanoHubSettings fields.
func (nhs *NanoHubSettings) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case nanohubsettings.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			nhs.ID = int(value.Int64)
		case nanohubsettings.FieldServerURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field server_url", values[i])
			} else if value.Valid {
				nhs.ServerURL = value.String
			}
		case nanohubsettings.FieldUsername:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field username", values[i])
			} else if value.Valid {
				nhs.Username = value.String
			}
		case nanohubsettings.FieldPassword:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field password", values[i])
			} else if value.Valid {
				nhs.Password = value.String
			}
		case nanohubsettings.FieldCaCerFile:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field ca_cer_file", values[i])
			} else if value.Valid {
				nhs.CaCerFile = value.String
			}
		case nanohubsettings.FieldScepURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field scep_url", values[i])
			} else if value.Valid {
				nhs.ScepURL = value.String
			}
		case nanohubsettings.FieldScepChallenge:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field scep_challenge", values[i])
			} else if value.Valid {
				nhs.ScepChallenge = value.String
			}
		case nanohubsettings.FieldMdmURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field mdm_url", values[i])
			} else if value.Valid {
				nhs.MdmURL = value.String
			}
		case nanohubsettings.FieldVendorPrivateKeyPem:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field vendor_private_key_pem", values[i])
			} else if value.Valid {
				nhs.VendorPrivateKeyPem = value.String
			}
		case nanohubsettings.FieldVendorCertPem:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field vendor_cert_pem", values[i])
			} else if value.Valid {
				nhs.VendorCertPem = value.String
			}
		case nanohubsettings.FieldEnrollmentProfileID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field enrollment_profile_id", values[i])
			} else if value.Valid {
				nhs.EnrollmentProfileID = value.String
			}
		default:
			nhs.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the NanoHubSettings.
// This includes values selected through modifiers, order, etc.
func (nhs *NanoHubSettings) Value(name string) (ent.Value, error) {
	return nhs.selectValues.Get(name)
}

// Update returns a builder for updating this NanoHubSettings.
// Note that you need to call NanoHubSettings.Unwrap() before calling this method if this NanoHubSettings
// was returned from a transaction, and the transaction was committed or rolled back.
func (nhs *NanoHubSettings) Update() *NanoHubSettingsUpdateOne {
	return NewNanoHubSettingsClient(nhs.config).UpdateOne(nhs)
}

// Unwrap unwraps the NanoHubSettings entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (nhs *NanoHubSettings) Unwrap() *NanoHubSettings {
	_tx, ok := nhs.config.driver.(*txDriver)
	if !ok {
		panic("ent: NanoHubSettings is not a transactional entity")
	}
	nhs.config.driver = _tx.drv
	return nhs
}

// String implements the fmt.Stringer.
func (nhs *NanoHubSettings) String() string {
	var builder strings.Builder
	builder.WriteString("NanoHubSettings(")
	builder.WriteString(fmt.Sprintf("id=%v, ", nhs.ID))
	builder.WriteString("server_url=")
	builder.WriteString(nhs.ServerURL)
	builder.WriteString(", ")
	builder.WriteString("username=")
	builder.WriteString(nhs.Username)
	builder.WriteString(", ")
	builder.WriteString("password=")
	builder.WriteString(nhs.Password)
	builder.WriteString(", ")
	builder.WriteString("ca_cer_file=")
	builder.WriteString(nhs.CaCerFile)
	builder.WriteString(", ")
	builder.WriteString("scep_url=")
	builder.WriteString(nhs.ScepURL)
	builder.WriteString(", ")
	builder.WriteString("scep_challenge=")
	builder.WriteString(nhs.ScepChallenge)
	builder.WriteString(", ")
	builder.WriteString("mdm_url=")
	builder.WriteString(nhs.MdmURL)
	builder.WriteString(", ")
	builder.WriteString("vendor_private_key_pem=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("vendor_cert_pem=")
	builder.WriteString(nhs.VendorCertPem)
	builder.WriteString(", ")
	builder.WriteString("enrollment_profile_id=")
	builder.WriteString(nhs.EnrollmentProfileID)
	builder.WriteByte(')')
	return builder.String()
}

// NanoHubSettingsSlice is a parsable slice of NanoHubSettings.
type NanoHubSettingsSlice []*NanoHubSettings
