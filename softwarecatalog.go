// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/EigerCode/ent/softwarecatalog"
	"github.com/EigerCode/ent/tenant"
)

// SoftwareCatalog is the model entity for the SoftwareCatalog schema.
type SoftwareCatalog struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Ring name: test, first, fast, broad
	Name string `json:"name,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// Rollout order: 0=test, 1=first, 2=fast, 3=broad
	RingOrder int `json:"ring_order,omitempty"`
	// IsDefault holds the value of the "is_default" field.
	IsDefault bool `json:"is_default,omitempty"`
	// Created holds the value of the "created" field.
	Created time.Time `json:"created,omitempty"`
	// Modified holds the value of the "modified" field.
	Modified time.Time `json:"modified,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SoftwareCatalogQuery when eager-loading is set.
	Edges                    SoftwareCatalogEdges `json:"edges"`
	tenant_software_catalogs *int
	selectValues             sql.SelectValues
}

// SoftwareCatalogEdges holds the relations/edges for other nodes in the graph.
type SoftwareCatalogEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// Packages holds the value of the packages edge.
	Packages []*ManagedPackage `json:"packages,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SoftwareCatalogEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// PackagesOrErr returns the Packages value or an error if the edge
// was not loaded in eager-loading.
func (e SoftwareCatalogEdges) PackagesOrErr() ([]*ManagedPackage, error) {
	if e.loadedTypes[1] {
		return e.Packages, nil
	}
	return nil, &NotLoadedError{edge: "packages"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SoftwareCatalog) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case softwarecatalog.FieldIsDefault:
			values[i] = new(sql.NullBool)
		case softwarecatalog.FieldID, softwarecatalog.FieldRingOrder:
			values[i] = new(sql.NullInt64)
		case softwarecatalog.FieldName, softwarecatalog.FieldDescription:
			values[i] = new(sql.NullString)
		case softwarecatalog.FieldCreated, softwarecatalog.FieldModified:
			values[i] = new(sql.NullTime)
		case softwarecatalog.ForeignKeys[0]: // tenant_software_catalogs
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SoftwareCatalog fields.
func (sc *SoftwareCatalog) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case softwarecatalog.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sc.ID = int(value.Int64)
		case softwarecatalog.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				sc.Name = value.String
			}
		case softwarecatalog.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				sc.Description = value.String
			}
		case softwarecatalog.FieldRingOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field ring_order", values[i])
			} else if value.Valid {
				sc.RingOrder = int(value.Int64)
			}
		case softwarecatalog.FieldIsDefault:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_default", values[i])
			} else if value.Valid {
				sc.IsDefault = value.Bool
			}
		case softwarecatalog.FieldCreated:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created", values[i])
			} else if value.Valid {
				sc.Created = value.Time
			}
		case softwarecatalog.FieldModified:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field modified", values[i])
			} else if value.Valid {
				sc.Modified = value.Time
			}
		case softwarecatalog.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field tenant_software_catalogs", value)
			} else if value.Valid {
				sc.tenant_software_catalogs = new(int)
				*sc.tenant_software_catalogs = int(value.Int64)
			}
		default:
			sc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SoftwareCatalog.
// This includes values selected through modifiers, order, etc.
func (sc *SoftwareCatalog) Value(name string) (ent.Value, error) {
	return sc.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the SoftwareCatalog entity.
func (sc *SoftwareCatalog) QueryTenant() *TenantQuery {
	return NewSoftwareCatalogClient(sc.config).QueryTenant(sc)
}

// QueryPackages queries the "packages" edge of the SoftwareCatalog entity.
func (sc *SoftwareCatalog) QueryPackages() *ManagedPackageQuery {
	return NewSoftwareCatalogClient(sc.config).QueryPackages(sc)
}

// Update returns a builder for updating this SoftwareCatalog.
// Note that you need to call SoftwareCatalog.Unwrap() before calling this method if this SoftwareCatalog
// was returned from a transaction, and the transaction was committed or rolled back.
func (sc *SoftwareCatalog) Update() *SoftwareCatalogUpdateOne {
	return NewSoftwareCatalogClient(sc.config).UpdateOne(sc)
}

// Unwrap unwraps the SoftwareCatalog entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sc *SoftwareCatalog) Unwrap() *SoftwareCatalog {
	_tx, ok := sc.config.driver.(*txDriver)
	if !ok {
		panic("ent: SoftwareCatalog is not a transactional entity")
	}
	sc.config.driver = _tx.drv
	return sc
}

// String implements the fmt.Stringer.
func (sc *SoftwareCatalog) String() string {
	var builder strings.Builder
	builder.WriteString("SoftwareCatalog(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sc.ID))
	builder.WriteString("name=")
	builder.WriteString(sc.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(sc.Description)
	builder.WriteString(", ")
	builder.WriteString("ring_order=")
	builder.WriteString(fmt.Sprintf("%v", sc.RingOrder))
	builder.WriteString(", ")
	builder.WriteString("is_default=")
	builder.WriteString(fmt.Sprintf("%v", sc.IsDefault))
	builder.WriteString(", ")
	builder.WriteString("created=")
	builder.WriteString(sc.Created.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("modified=")
	builder.WriteString(sc.Modified.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SoftwareCatalogs is a parsable slice of SoftwareCatalog.
type SoftwareCatalogs []*SoftwareCatalog
