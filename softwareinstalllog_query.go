// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/EigerCode/ent/agent"
	"github.com/EigerCode/ent/managedpackage"
	"github.com/EigerCode/ent/predicate"
	"github.com/EigerCode/ent/softwareinstalllog"
)

// SoftwareInstallLogQuery is the builder for querying SoftwareInstallLog entities.
type SoftwareInstallLogQuery struct {
	config
	ctx         *QueryContext
	order       []softwareinstalllog.OrderOption
	inters      []Interceptor
	predicates  []predicate.SoftwareInstallLog
	withAgent   *AgentQuery
	withPackage *ManagedPackageQuery
	withFKs     bool
	modifiers   []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SoftwareInstallLogQuery builder.
func (silq *SoftwareInstallLogQuery) Where(ps ...predicate.SoftwareInstallLog) *SoftwareInstallLogQuery {
	silq.predicates = append(silq.predicates, ps...)
	return silq
}

// Limit the number of records to be returned by this query.
func (silq *SoftwareInstallLogQuery) Limit(limit int) *SoftwareInstallLogQuery {
	silq.ctx.Limit = &limit
	return silq
}

// Offset to start from.
func (silq *SoftwareInstallLogQuery) Offset(offset int) *SoftwareInstallLogQuery {
	silq.ctx.Offset = &offset
	return silq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (silq *SoftwareInstallLogQuery) Unique(unique bool) *SoftwareInstallLogQuery {
	silq.ctx.Unique = &unique
	return silq
}

// Order specifies how the records should be ordered.
func (silq *SoftwareInstallLogQuery) Order(o ...softwareinstalllog.OrderOption) *SoftwareInstallLogQuery {
	silq.order = append(silq.order, o...)
	return silq
}

// QueryAgent chains the current query on the "agent" edge.
func (silq *SoftwareInstallLogQuery) QueryAgent() *AgentQuery {
	query := (&AgentClient{config: silq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := silq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := silq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(softwareinstalllog.Table, softwareinstalllog.FieldID, selector),
			sqlgraph.To(agent.Table, agent.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, softwareinstalllog.AgentTable, softwareinstalllog.AgentColumn),
		)
		fromU = sqlgraph.SetNeighbors(silq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPackage chains the current query on the "package" edge.
func (silq *SoftwareInstallLogQuery) QueryPackage() *ManagedPackageQuery {
	query := (&ManagedPackageClient{config: silq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := silq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := silq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(softwareinstalllog.Table, softwareinstalllog.FieldID, selector),
			sqlgraph.To(managedpackage.Table, managedpackage.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, softwareinstalllog.PackageTable, softwareinstalllog.PackageColumn),
		)
		fromU = sqlgraph.SetNeighbors(silq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SoftwareInstallLog entity from the query.
// Returns a *NotFoundError when no SoftwareInstallLog was found.
func (silq *SoftwareInstallLogQuery) First(ctx context.Context) (*SoftwareInstallLog, error) {
	nodes, err := silq.Limit(1).All(setContextOp(ctx, silq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{softwareinstalllog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (silq *SoftwareInstallLogQuery) FirstX(ctx context.Context) *SoftwareInstallLog {
	node, err := silq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SoftwareInstallLog ID from the query.
// Returns a *NotFoundError when no SoftwareInstallLog ID was found.
func (silq *SoftwareInstallLogQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = silq.Limit(1).IDs(setContextOp(ctx, silq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{softwareinstalllog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (silq *SoftwareInstallLogQuery) FirstIDX(ctx context.Context) int {
	id, err := silq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SoftwareInstallLog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SoftwareInstallLog entity is found.
// Returns a *NotFoundError when no SoftwareInstallLog entities are found.
func (silq *SoftwareInstallLogQuery) Only(ctx context.Context) (*SoftwareInstallLog, error) {
	nodes, err := silq.Limit(2).All(setContextOp(ctx, silq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{softwareinstalllog.Label}
	default:
		return nil, &NotSingularError{softwareinstalllog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (silq *SoftwareInstallLogQuery) OnlyX(ctx context.Context) *SoftwareInstallLog {
	node, err := silq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SoftwareInstallLog ID in the query.
// Returns a *NotSingularError when more than one SoftwareInstallLog ID is found.
// Returns a *NotFoundError when no entities are found.
func (silq *SoftwareInstallLogQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = silq.Limit(2).IDs(setContextOp(ctx, silq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{softwareinstalllog.Label}
	default:
		err = &NotSingularError{softwareinstalllog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (silq *SoftwareInstallLogQuery) OnlyIDX(ctx context.Context) int {
	id, err := silq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SoftwareInstallLogs.
func (silq *SoftwareInstallLogQuery) All(ctx context.Context) ([]*SoftwareInstallLog, error) {
	ctx = setContextOp(ctx, silq.ctx, ent.OpQueryAll)
	if err := silq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SoftwareInstallLog, *SoftwareInstallLogQuery]()
	return withInterceptors[[]*SoftwareInstallLog](ctx, silq, qr, silq.inters)
}

// AllX is like All, but panics if an error occurs.
func (silq *SoftwareInstallLogQuery) AllX(ctx context.Context) []*SoftwareInstallLog {
	nodes, err := silq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SoftwareInstallLog IDs.
func (silq *SoftwareInstallLogQuery) IDs(ctx context.Context) (ids []int, err error) {
	if silq.ctx.Unique == nil && silq.path != nil {
		silq.Unique(true)
	}
	ctx = setContextOp(ctx, silq.ctx, ent.OpQueryIDs)
	if err = silq.Select(softwareinstalllog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (silq *SoftwareInstallLogQuery) IDsX(ctx context.Context) []int {
	ids, err := silq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (silq *SoftwareInstallLogQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, silq.ctx, ent.OpQueryCount)
	if err := silq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, silq, querierCount[*SoftwareInstallLogQuery](), silq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (silq *SoftwareInstallLogQuery) CountX(ctx context.Context) int {
	count, err := silq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (silq *SoftwareInstallLogQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, silq.ctx, ent.OpQueryExist)
	switch _, err := silq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (silq *SoftwareInstallLogQuery) ExistX(ctx context.Context) bool {
	exist, err := silq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SoftwareInstallLogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (silq *SoftwareInstallLogQuery) Clone() *SoftwareInstallLogQuery {
	if silq == nil {
		return nil
	}
	return &SoftwareInstallLogQuery{
		config:      silq.config,
		ctx:         silq.ctx.Clone(),
		order:       append([]softwareinstalllog.OrderOption{}, silq.order...),
		inters:      append([]Interceptor{}, silq.inters...),
		predicates:  append([]predicate.SoftwareInstallLog{}, silq.predicates...),
		withAgent:   silq.withAgent.Clone(),
		withPackage: silq.withPackage.Clone(),
		// clone intermediate query.
		sql:       silq.sql.Clone(),
		path:      silq.path,
		modifiers: append([]func(*sql.Selector){}, silq.modifiers...),
	}
}

// WithAgent tells the query-builder to eager-load the nodes that are connected to
// the "agent" edge. The optional arguments are used to configure the query builder of the edge.
func (silq *SoftwareInstallLogQuery) WithAgent(opts ...func(*AgentQuery)) *SoftwareInstallLogQuery {
	query := (&AgentClient{config: silq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	silq.withAgent = query
	return silq
}

// WithPackage tells the query-builder to eager-load the nodes that are connected to
// the "package" edge. The optional arguments are used to configure the query builder of the edge.
func (silq *SoftwareInstallLogQuery) WithPackage(opts ...func(*ManagedPackageQuery)) *SoftwareInstallLogQuery {
	query := (&ManagedPackageClient{config: silq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	silq.withPackage = query
	return silq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Action softwareinstalllog.Action `json:"action,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SoftwareInstallLog.Query().
//		GroupBy(softwareinstalllog.FieldAction).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (silq *SoftwareInstallLogQuery) GroupBy(field string, fields ...string) *SoftwareInstallLogGroupBy {
	silq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SoftwareInstallLogGroupBy{build: silq}
	grbuild.flds = &silq.ctx.Fields
	grbuild.label = softwareinstalllog.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Action softwareinstalllog.Action `json:"action,omitempty"`
//	}
//
//	client.SoftwareInstallLog.Query().
//		Select(softwareinstalllog.FieldAction).
//		Scan(ctx, &v)
func (silq *SoftwareInstallLogQuery) Select(fields ...string) *SoftwareInstallLogSelect {
	silq.ctx.Fields = append(silq.ctx.Fields, fields...)
	sbuild := &SoftwareInstallLogSelect{SoftwareInstallLogQuery: silq}
	sbuild.label = softwareinstalllog.Label
	sbuild.flds, sbuild.scan = &silq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SoftwareInstallLogSelect configured with the given aggregations.
func (silq *SoftwareInstallLogQuery) Aggregate(fns ...AggregateFunc) *SoftwareInstallLogSelect {
	return silq.Select().Aggregate(fns...)
}

func (silq *SoftwareInstallLogQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range silq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, silq); err != nil {
				return err
			}
		}
	}
	for _, f := range silq.ctx.Fields {
		if !softwareinstalllog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if silq.path != nil {
		prev, err := silq.path(ctx)
		if err != nil {
			return err
		}
		silq.sql = prev
	}
	return nil
}

func (silq *SoftwareInstallLogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SoftwareInstallLog, error) {
	var (
		nodes       = []*SoftwareInstallLog{}
		withFKs     = silq.withFKs
		_spec       = silq.querySpec()
		loadedTypes = [2]bool{
			silq.withAgent != nil,
			silq.withPackage != nil,
		}
	)
	if silq.withAgent != nil || silq.withPackage != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, softwareinstalllog.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SoftwareInstallLog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SoftwareInstallLog{config: silq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(silq.modifiers) > 0 {
		_spec.Modifiers = silq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, silq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := silq.withAgent; query != nil {
		if err := silq.loadAgent(ctx, query, nodes, nil,
			func(n *SoftwareInstallLog, e *Agent) { n.Edges.Agent = e }); err != nil {
			return nil, err
		}
	}
	if query := silq.withPackage; query != nil {
		if err := silq.loadPackage(ctx, query, nodes, nil,
			func(n *SoftwareInstallLog, e *ManagedPackage) { n.Edges.Package = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (silq *SoftwareInstallLogQuery) loadAgent(ctx context.Context, query *AgentQuery, nodes []*SoftwareInstallLog, init func(*SoftwareInstallLog), assign func(*SoftwareInstallLog, *Agent)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*SoftwareInstallLog)
	for i := range nodes {
		if nodes[i].agent_software_install_logs == nil {
			continue
		}
		fk := *nodes[i].agent_software_install_logs
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(agent.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "agent_software_install_logs" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (silq *SoftwareInstallLogQuery) loadPackage(ctx context.Context, query *ManagedPackageQuery, nodes []*SoftwareInstallLog, init func(*SoftwareInstallLog), assign func(*SoftwareInstallLog, *ManagedPackage)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*SoftwareInstallLog)
	for i := range nodes {
		if nodes[i].managed_package_install_logs == nil {
			continue
		}
		fk := *nodes[i].managed_package_install_logs
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(managedpackage.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "managed_package_install_logs" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (silq *SoftwareInstallLogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := silq.querySpec()
	if len(silq.modifiers) > 0 {
		_spec.Modifiers = silq.modifiers
	}
	_spec.Node.Columns = silq.ctx.Fields
	if len(silq.ctx.Fields) > 0 {
		_spec.Unique = silq.ctx.Unique != nil && *silq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, silq.driver, _spec)
}

func (silq *SoftwareInstallLogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(softwareinstalllog.Table, softwareinstalllog.Columns, sqlgraph.NewFieldSpec(softwareinstalllog.FieldID, field.TypeInt))
	_spec.From = silq.sql
	if unique := silq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if silq.path != nil {
		_spec.Unique = true
	}
	if fields := silq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, softwareinstalllog.FieldID)
		for i := range fields {
			if fields[i] != softwareinstalllog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := silq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := silq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := silq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := silq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (silq *SoftwareInstallLogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(silq.driver.Dialect())
	t1 := builder.Table(softwareinstalllog.Table)
	columns := silq.ctx.Fields
	if len(columns) == 0 {
		columns = softwareinstalllog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if silq.sql != nil {
		selector = silq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if silq.ctx.Unique != nil && *silq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range silq.modifiers {
		m(selector)
	}
	for _, p := range silq.predicates {
		p(selector)
	}
	for _, p := range silq.order {
		p(selector)
	}
	if offset := silq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := silq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (silq *SoftwareInstallLogQuery) Modify(modifiers ...func(s *sql.Selector)) *SoftwareInstallLogSelect {
	silq.modifiers = append(silq.modifiers, modifiers...)
	return silq.Select()
}

// SoftwareInstallLogGroupBy is the group-by builder for SoftwareInstallLog entities.
type SoftwareInstallLogGroupBy struct {
	selector
	build *SoftwareInstallLogQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (silgb *SoftwareInstallLogGroupBy) Aggregate(fns ...AggregateFunc) *SoftwareInstallLogGroupBy {
	silgb.fns = append(silgb.fns, fns...)
	return silgb
}

// Scan applies the selector query and scans the result into the given value.
func (silgb *SoftwareInstallLogGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, silgb.build.ctx, ent.OpQueryGroupBy)
	if err := silgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SoftwareInstallLogQuery, *SoftwareInstallLogGroupBy](ctx, silgb.build, silgb, silgb.build.inters, v)
}

func (silgb *SoftwareInstallLogGroupBy) sqlScan(ctx context.Context, root *SoftwareInstallLogQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(silgb.fns))
	for _, fn := range silgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*silgb.flds)+len(silgb.fns))
		for _, f := range *silgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*silgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := silgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SoftwareInstallLogSelect is the builder for selecting fields of SoftwareInstallLog entities.
type SoftwareInstallLogSelect struct {
	*SoftwareInstallLogQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sils *SoftwareInstallLogSelect) Aggregate(fns ...AggregateFunc) *SoftwareInstallLogSelect {
	sils.fns = append(sils.fns, fns...)
	return sils
}

// Scan applies the selector query and scans the result into the given value.
func (sils *SoftwareInstallLogSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sils.ctx, ent.OpQuerySelect)
	if err := sils.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SoftwareInstallLogQuery, *SoftwareInstallLogSelect](ctx, sils.SoftwareInstallLogQuery, sils, sils.inters, v)
}

func (sils *SoftwareInstallLogSelect) sqlScan(ctx context.Context, root *SoftwareInstallLogQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sils.fns))
	for _, fn := range sils.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sils.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sils.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (sils *SoftwareInstallLogSelect) Modify(modifiers ...func(s *sql.Selector)) *SoftwareInstallLogSelect {
	sils.modifiers = append(sils.modifiers, modifiers...)
	return sils
}
