// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/EigerCode/ent/softwareassignment"
	"github.com/EigerCode/ent/tenant"
)

// SoftwareAssignment is the model entity for the SoftwareAssignment schema.
type SoftwareAssignment struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Munki/CIMIAN package name (e.g. Firefox)
	PackageName string `json:"package_name,omitempty"`
	// Platform of the package
	PackagePlatform softwareassignment.PackagePlatform `json:"package_platform,omitempty"`
	// AssignmentType holds the value of the "assignment_type" field.
	AssignmentType softwareassignment.AssignmentType `json:"assignment_type,omitempty"`
	// What kind of target this assignment applies to
	TargetType softwareassignment.TargetType `json:"target_type,omitempty"`
	// ID of the target (site ID, tag ID, or agent ID)
	TargetID string `json:"target_id,omitempty"`
	// Higher priority wins when there are conflicts
	Priority int `json:"priority,omitempty"`
	// Optional JSON condition, e.g. OS version, architecture
	ConditionPredicate string `json:"condition_predicate,omitempty"`
	// Active holds the value of the "active" field.
	Active bool `json:"active,omitempty"`
	// Created holds the value of the "created" field.
	Created time.Time `json:"created,omitempty"`
	// Modified holds the value of the "modified" field.
	Modified time.Time `json:"modified,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SoftwareAssignmentQuery when eager-loading is set.
	Edges                       SoftwareAssignmentEdges `json:"edges"`
	tenant_software_assignments *int
	selectValues                sql.SelectValues
}

// SoftwareAssignmentEdges holds the relations/edges for other nodes in the graph.
type SoftwareAssignmentEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SoftwareAssignmentEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SoftwareAssignment) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case softwareassignment.FieldActive:
			values[i] = new(sql.NullBool)
		case softwareassignment.FieldID, softwareassignment.FieldPriority:
			values[i] = new(sql.NullInt64)
		case softwareassignment.FieldPackageName, softwareassignment.FieldPackagePlatform, softwareassignment.FieldAssignmentType, softwareassignment.FieldTargetType, softwareassignment.FieldTargetID, softwareassignment.FieldConditionPredicate:
			values[i] = new(sql.NullString)
		case softwareassignment.FieldCreated, softwareassignment.FieldModified:
			values[i] = new(sql.NullTime)
		case softwareassignment.ForeignKeys[0]: // tenant_software_assignments
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SoftwareAssignment fields.
func (sa *SoftwareAssignment) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case softwareassignment.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sa.ID = int(value.Int64)
		case softwareassignment.FieldPackageName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field package_name", values[i])
			} else if value.Valid {
				sa.PackageName = value.String
			}
		case softwareassignment.FieldPackagePlatform:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field package_platform", values[i])
			} else if value.Valid {
				sa.PackagePlatform = softwareassignment.PackagePlatform(value.String)
			}
		case softwareassignment.FieldAssignmentType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field assignment_type", values[i])
			} else if value.Valid {
				sa.AssignmentType = softwareassignment.AssignmentType(value.String)
			}
		case softwareassignment.FieldTargetType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field target_type", values[i])
			} else if value.Valid {
				sa.TargetType = softwareassignment.TargetType(value.String)
			}
		case softwareassignment.FieldTargetID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field target_id", values[i])
			} else if value.Valid {
				sa.TargetID = value.String
			}
		case softwareassignment.FieldPriority:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field priority", values[i])
			} else if value.Valid {
				sa.Priority = int(value.Int64)
			}
		case softwareassignment.FieldConditionPredicate:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field condition_predicate", values[i])
			} else if value.Valid {
				sa.ConditionPredicate = value.String
			}
		case softwareassignment.FieldActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field active", values[i])
			} else if value.Valid {
				sa.Active = value.Bool
			}
		case softwareassignment.FieldCreated:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created", values[i])
			} else if value.Valid {
				sa.Created = value.Time
			}
		case softwareassignment.FieldModified:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field modified", values[i])
			} else if value.Valid {
				sa.Modified = value.Time
			}
		case softwareassignment.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field tenant_software_assignments", value)
			} else if value.Valid {
				sa.tenant_software_assignments = new(int)
				*sa.tenant_software_assignments = int(value.Int64)
			}
		default:
			sa.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SoftwareAssignment.
// This includes values selected through modifiers, order, etc.
func (sa *SoftwareAssignment) Value(name string) (ent.Value, error) {
	return sa.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the SoftwareAssignment entity.
func (sa *SoftwareAssignment) QueryTenant() *TenantQuery {
	return NewSoftwareAssignmentClient(sa.config).QueryTenant(sa)
}

// Update returns a builder for updating this SoftwareAssignment.
// Note that you need to call SoftwareAssignment.Unwrap() before calling this method if this SoftwareAssignment
// was returned from a transaction, and the transaction was committed or rolled back.
func (sa *SoftwareAssignment) Update() *SoftwareAssignmentUpdateOne {
	return NewSoftwareAssignmentClient(sa.config).UpdateOne(sa)
}

// Unwrap unwraps the SoftwareAssignment entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sa *SoftwareAssignment) Unwrap() *SoftwareAssignment {
	_tx, ok := sa.config.driver.(*txDriver)
	if !ok {
		panic("ent: SoftwareAssignment is not a transactional entity")
	}
	sa.config.driver = _tx.drv
	return sa
}

// String implements the fmt.Stringer.
func (sa *SoftwareAssignment) String() string {
	var builder strings.Builder
	builder.WriteString("SoftwareAssignment(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sa.ID))
	builder.WriteString("package_name=")
	builder.WriteString(sa.PackageName)
	builder.WriteString(", ")
	builder.WriteString("package_platform=")
	builder.WriteString(fmt.Sprintf("%v", sa.PackagePlatform))
	builder.WriteString(", ")
	builder.WriteString("assignment_type=")
	builder.WriteString(fmt.Sprintf("%v", sa.AssignmentType))
	builder.WriteString(", ")
	builder.WriteString("target_type=")
	builder.WriteString(fmt.Sprintf("%v", sa.TargetType))
	builder.WriteString(", ")
	builder.WriteString("target_id=")
	builder.WriteString(sa.TargetID)
	builder.WriteString(", ")
	builder.WriteString("priority=")
	builder.WriteString(fmt.Sprintf("%v", sa.Priority))
	builder.WriteString(", ")
	builder.WriteString("condition_predicate=")
	builder.WriteString(sa.ConditionPredicate)
	builder.WriteString(", ")
	builder.WriteString("active=")
	builder.WriteString(fmt.Sprintf("%v", sa.Active))
	builder.WriteString(", ")
	builder.WriteString("created=")
	builder.WriteString(sa.Created.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("modified=")
	builder.WriteString(sa.Modified.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SoftwareAssignments is a parsable slice of SoftwareAssignment.
type SoftwareAssignments []*SoftwareAssignment
